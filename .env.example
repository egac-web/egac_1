# Example .env.example — do NOT commit secrets here. Replace with real values in your environment/secret manager.

# Directus
DIRECTUS_URL=https://your-directus.example
DIRECTUS_TOKEN=replace-with-directus-token

# Airtable (optional)
AIRTABLE_API_KEY=replace-with-airtable-key
AIRTABLE_BASE=replace-with-base-id
AIRTABLE_TABLE=Enquiries

# Supabase
SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
SUPABASE_ANON_KEY=replace-with-anon-key

# Resend (email sending)
RESEND_API_KEY=replace-with-resend-api-key
RESEND_FROM="EGAC <enquiries@example.com>"

# Admin token for CI / cron endpoints
ADMIN_TOKEN=replace-with-admin-token

# Misc
MEMBERSHIP_SECRETARY_EMAIL=membership@eastgrinsteadac.co.uk
SITE_BASE_URL=https://egac.example
CRON_SECRET=replace-with-strong-cron-secret

# Cloudflare
CLOUDFLARE_API_TOKEN=replace-with-cloudflare-token

# Notes:
# - Store real secrets in your CI/hosting provider (GitHub Actions secrets, Cloudflare Pages env, etc.)
# - Do not paste real secrets into PRs or public channels.